name: First Worker
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello world!"
          echo "Good morning"
      - name: Print goodbye
        run: echo "Done - bye!"
